import { describe, expect, it } from "vitest";

describe("QuestionSelector", () => {
  it("selects all questions when under the limit", () => {});

  it("limits the number of selected questions", () => {});

  it("returns at most two questions per concept", () => {});

  it("prefers lower difficulty questions", () => {});

  it("returns questions for a specific concept", () => {});

  it("returns an empty array for an unknown concept", () => {});

  it("returns all questions", () => {});

  it("returns an empty array when the repository is empty", () => {});
});