import { describe, expect, it } from "vitest";


describe("AssessmentSessionManager", () => {
  it("starts a new session", () => {});

  it("returns the current question", () => {});

  it("submits an answer", () => {});

  it("moves to the next question", () => {});

  it("tracks progress", () => {});

  it("finishes a session", () => {});

  it("reports active state", () => {});

  it("throws when accessing session before start", () => {});

  it("returns undefined after the final question", () => {});
});